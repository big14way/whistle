// Ported 1:1 from app/src/theme/tokens.css so the video reads as the same product
// as the app, not a separate marketing asset. Keep these two files in sync by hand;
// there are few enough values that a build step would be overkill.

export const color = {
  bg: "#0a0e14",
  surface: "#121821",
  surface2: "#1a2230",
  border: "#232c3b",
  text: "#e8edf4",
  textMuted: "#8a97a8",
  yes: "#00e5a0",
  no: "#ff5c5c",
  accent: "#4d7cff",
  pending: "#ffb84d",
};

export const radius = {
  card: 16,
  btn: 10,
  pill: 999,
};

export const font = {
  display: "Space Grotesk",
  body: "Inter",
  mono: "JetBrains Mono",
};
