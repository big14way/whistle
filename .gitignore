# Rust / Anchor
target/
**/*.rs.bk
# Cargo.lock is committed on purpose: it pins blake3 to 1.5.5 so the SBF rustc
# (1.79, platform-tools v1.43) does not pull edition2024 preview crypto crates.

# Node
node_modules/
.pnpm-store/
dist/
.vite/

# Anchor
.anchor/
test-ledger/

# Generated config and secrets (never commit keys)
app/src/config.generated.json
app/src/demo-wallets.generated.json
.txline-token-cache.json
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# transient build helper files
*.pid
anchor-build.log
sbf-test.log
*.tsbuildinfo
app/vite.config.js
app/vite.config.d.ts

# local Claude Code session state
.claude/

# injected demo tokens (gitignored secret)
app/src/txline-tokens.generated.json
